=link_to "Sign out", destroy_promoter_session_path, :method => 'delete'

#photo

#employees
  =link_to "Add", new_employee_invitation_path, remote: true
  #employees-container

#venues
  =link_to "Add", new_venue_path, remote: true
  #venues-container
    = render 'venues/index'

#concerts
  #upcoming-concerts
    =link_to "Add", new_concert_path, remote: true
    #concerts-container
      = render 'concerts/index'

